import {
	StyleSheet
} from 'react-native';

export default Styles = StyleSheet.create({
  mainContainer: {
    flex: 1,
    backgroundColor: '#333333',
    paddingTop: 30
  },
  viewContainer: {
    justifyContent: 'center',
    alignItems: 'center'
  }
});

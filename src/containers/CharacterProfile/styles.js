import {
	StyleSheet
} from 'react-native';

export default Styles = StyleSheet.create({
  mainContainer: {
		backgroundColor: '#222',
		flex: 1
  },
	scrollContainer: {
		backgroundColor: '#960b11'
	}
});
